# Environment Variables for Building RAG Agents with LLMs
# Copy this file to .env and replace with your actual values

# Required: NVIDIA API Key for accessing NVIDIA NIM endpoints
# Get your API key from: https://build.nvidia.com/
NVIDIA_API_KEY=your_nvidia_api_key_here

# Optional: Override default LLM client settings
NVIDIA_BASE_URL=http://llm_client:9000/v1
NVIDIA_DEFAULT_MODE=open

# Optional: OpenAI API Key (if you want to use OpenAI models)
# OPENAI_API_KEY=your_openai_api_key_here

# Docker-specific settings (usually don't need to change)
# DOCKER_BUILDKIT=1
# COMPOSE_DOCKER_CLI_BUILD=1
